# RAG Agent dependencies
# Python 3.10+

# LlamaIndex core
llama-index-core>=0.11.0

# LLM — Ollama
llama-index-llms-ollama>=0.3.0

# Embeddings — Ollama (nomic-embed-text)
llama-index-embeddings-ollama>=0.3.0

# Vector store — Qdrant
llama-index-vector-stores-qdrant>=0.3.0
qdrant-client>=1.9.0

# BM25 retriever
llama-index-retrievers-bm25>=0.3.0
rank-bm25>=0.2.2

# Document parsers
docling>=2.111.0          # structured DOCX/PDF/PPTX ingestion for RAG
mammoth>=1.12.0           # lightweight DOCX to Markdown fallback
# Optional heavier fallback:
# unstructured>=0.18.0
llama-index-readers-file>=0.2.0
python-docx>=1.1.0       # DOCX
docx2txt>=0.8            # DOCX fallback for LlamaIndex readers
pypdf>=4.0.0             # PDF
pymupdf>=1.24.0          # PDF (лучше чем pypdf для сложных)

# Прочее
nltk>=3.8.0              # нужен BM25Retriever для токенизации

# API / security / observability
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
prometheus-client>=0.20.0
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
